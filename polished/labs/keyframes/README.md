# Keyframes

Learners store poses, scrub between them, compare interpolation, and author a short animation. `actor.js` supplies the subject while `animation.js` and `beats.js` drive the lesson. Serve the repository and run `node scripts/qa-polished-curriculum.mjs` after edits.
