# Particles

Learners build a readable spark fountain from emitter rate, lifetime, forces, and pooling. `particles.js` is the simulation; `readout.js` exposes its state and `beats.js` stages the exercises. Serve the repository and run the polished curriculum QA after edits.
