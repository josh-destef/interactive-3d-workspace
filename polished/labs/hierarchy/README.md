# Hierarchy

This in-progress lesson teaches parent-child transforms with a modeled arm, local/world space, and reparenting through a pickup task. `arm.js` defines the subject; `config.js`, `beats.js`, and `stage.js` control the staged shell. It carries local kit-era infrastructure, so avoid adding another copy. Serve the repository and run the polished curriculum QA after edits.
