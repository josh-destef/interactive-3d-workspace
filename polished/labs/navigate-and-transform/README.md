# Navigate + Transform

The curriculum entry point: learners orbit Gizmobot, then use translation, rotation, and scale gizmos. `js/config.js` loads the canonical `../../../assets/models/gizmobot.glb`; `main.js`, `interaction.js`, and `gizmos.js` run the scene. Serve the repository and open this folder's `index.html`.
