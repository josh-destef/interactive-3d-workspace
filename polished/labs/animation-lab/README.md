# Animation Lab

Learners pose Gizmobot at keyframes and play a simple wave. `model.js` and `scene.js` supply the rigged assembly model; `main.js` owns timeline behavior. It reuses Robot Assembly assets rather than copying another full robot. Serve the repository and run `node scripts/qa-animation-lab.mjs` after changes.
