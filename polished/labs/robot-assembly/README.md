# Build Gizmobot

Learners assemble fourteen Gizmobot parts, then pose a rigid joint hierarchy. `main.js` loads the manifest and model; `posing.js` handles posing. The two local GLBs are intentional generated variants; see `ASSET-NOTES.md`. Rebuild with `python scripts/build-assembly-gizmobot.py` and run the related QA scripts.
